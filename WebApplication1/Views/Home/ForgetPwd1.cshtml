// 使用 Google Mail Server 發信
string GoogleID = "f50318acer@gmail.com"; //Google 發信帳號
string TempPwd = "merb dubm nuta rmdy"; //應用程式密碼
string ReceiveMail = "f50318acer@gmail.com"; //接收信箱

string SmtpServer = "smtp.gmail.com";
int SmtpPort = 587;
MailMessage mms = new MailMessage();
mms.From = new MailAddress(GoogleID);
mms.Subject = "信件主題";
mms.Body = "信件內容";
mms.IsBodyHtml = true;
mms.SubjectEncoding = Encoding.UTF8;
mms.To.Add(new MailAddress(ReceiveMail));
using (SmtpClient client = new SmtpClient(SmtpServer, SmtpPort))
{
	client.EnableSsl = true;
	client.Credentials = new NetworkCredential(GoogleID, TempPwd);//寄信帳密
	client.Send(mms); //寄出信件
}